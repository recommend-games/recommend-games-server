version: '3'

services:
    web:
        image: ludoj-server:latest
        container_name: ludoj
        build:
            context: '.'
            dockerfile: Dockerfile
        # depends_on:
        #     - db
        environment:
            - PORT=5000
        ports:
            - '5000:5000'
        # volumes:
        #     - ./output:/root/output
        restart: always
        networks:
            - ludoj

networks:
    ludoj:
        driver: bridge
